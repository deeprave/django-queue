[project]
name = "django-queues-priority-demo"
version = "0.0.1"
description = "Minimal Django dashboard for the django-queues priority queue observer demo"
requires-python = ">=3.14"
dependencies = [
    "django-queues[redis]",
    "faker>=40.36.0",
]

[tool.uv.sources]
django-queues = { path = "..", editable = true }
