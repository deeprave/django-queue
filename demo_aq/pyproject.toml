[project]
name = "django-queue-async-demo"
version = "0.0.1"
description = "Minimal Django dashboard for the django-queue async queue observer demo"
requires-python = ">=3.14"
dependencies = [
    "django-queue",
    "faker>=40.36.0",
    "redis>=5.2.1",
]

[tool.uv.sources]
django-queue = { path = "..", editable = true }
