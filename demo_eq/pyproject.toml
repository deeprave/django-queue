[project]
name = "django-queues-event-demo"
version = "0.0.1"
description = "Minimal Django dashboard for the django-queues event queue demo"
requires-python = ">=3.14"
dependencies = ["django-queues[redis]"]

[tool.uv.sources]
django-queues = { path = "..", editable = true }
