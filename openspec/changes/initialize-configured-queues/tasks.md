## 1. Configured queue registry

- [ ] 1.1 Write failing tests for successful, idempotent, and invalid `QUEUES` initialization.
- [ ] 1.2 Implement app-startup registry initialization and setting validation.
- [ ] 1.3 Verify no worker starts during initialization and document `QUEUES` access.
